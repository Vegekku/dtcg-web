const modalOpen = () => {
    const modal = document.getElementById('modal-one');
    modal.classList.add('open');
};

const modalClose = () => {
    const modal = document.getElementById('modal-one');
    modal.classList.remove('open');
}